// Dump of all post-preprocessor input
// Blank lines and `line directives have been removed
//
// Information:
//   Version: Verilator 5.045 devel rev v5.044-47-g09ca7ea8d
//   Arguments: --debug --cc --trace -MMD -O0 vsrc/example.v --exe csrc/example.cpp --Mdir build/example --build /home/biruide/ysyx-workbench/nvboard/build/nvboard.a -CFLAGS -MMD -CFLAGS -O3 -CFLAGS -I/usr/include/SDL2 -CFLAGS -D_REENTRANT -CFLAGS -I/home/biruide/ysyx-workbench/nvboard/usr/include -LDFLAGS -lSDL2 -LDFLAGS -lSDL2_image -LDFLAGS -lSDL2_ttf
//   Build jobs: 1
//   Verilate jobs: 1

// verilator fargs --cc
// verilator fargs --trace
// verilator fargs -MMD
// verilator fargs -O0
// verilator fargs --exe
// verilator fargs csrc/example.cpp
// verilator fargs --build
// verilator fargs /home/biruide/ysyx-workbench/nvboard/build/nvboard.a
// verilator fargs -CFLAGS -MMD
// verilator fargs -CFLAGS -O3
// verilator fargs -CFLAGS -I/usr/include/SDL2
// verilator fargs -CFLAGS -D_REENTRANT
// verilator fargs -CFLAGS -I/home/biruide/ysyx-workbench/nvboard/usr/include
// verilator fargs -LDFLAGS -lSDL2
// verilator fargs -LDFLAGS -lSDL2_image
// verilator fargs -LDFLAGS -lSDL2_ttf

`begin_keywords "1800-2023"
module example();
endmodule
